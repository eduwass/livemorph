{
  "name": "livemorph",
  "module": "index.ts",
  "type": "module",
  "scripts": {
    "build": "bun build --compile --minify --sourcemap --bytecode ./index.ts --outfile livemorph"
  },
  "dependencies": {
    "bun-sse": "file:packages/bun-sse"
  },
  "devDependencies": {
    "@types/bun": "latest"
  },
  "peerDependencies": {
    "typescript": "^5"
  }
}
